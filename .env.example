# Root .env.example — copy relevant sections into subfolder .env files
# See frontend/.env.example and backend/.env.example for per-service keys

# Frontend (copy to frontend/.env)
VITE_GOOGLE_MAPS_API_KEY=
VITE_MAPBOX_TOKEN=

# Backend (copy to backend/.env)
ANTHROPIC_API_KEY=
EPD_API_TOKEN=
