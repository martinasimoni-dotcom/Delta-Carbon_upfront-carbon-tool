using Eto.Forms;

namespace SurroundPlugin.UI
{
    /// <summary>
    /// Baseline vs alternative scenario comparison view — Phase 2 feature.
    /// Stub present so the project compiles; full implementation in Phase 2.
    /// </summary>
    public class ScenarioComparePanel : Panel
    {
        public ScenarioComparePanel()
        {
            Content = new Label
            {
                Text      = "Scenario comparison — Phase 2.",
                TextColor = Eto.Drawing.Colors.Gray
            };
        }
    }
}
